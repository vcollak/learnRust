//every program must have a main function
fn main() {
    //define a string called name and assign "world" to it
    let name = "world";

    //print the string with the name
    println!("hello {}", name);
}
